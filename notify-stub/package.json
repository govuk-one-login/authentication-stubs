{
  "name": "notify-stub",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "start:local": "sam build && sam local start-api --parameter-overrides 'Environment=local' --warm-containers EAGER",
    "test-unit": "NODE_OPTIONS=--experimental-vm-modules jest",
    "lint": "echo 'No linter configured'",
    "pretty": "prettier --write '**/*.ts'",
    "pretty-check": "prettier --check '**/*.ts'"
  },
  "dependencies": {
    "@aws-sdk/client-dynamodb": "3.1110.0",
    "@aws-sdk/lib-dynamodb": "3.1110.0",
    "pino": "10.3.1"
  },
  "devDependencies": {
    "@types/aws-lambda": "8.10.145",
    "@types/node": "24.0.3",
    "esbuild": "0.28.1",
    "prettier": "3.9.6",
    "typescript": "7.0.2"
  }
}
